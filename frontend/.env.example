# Public backend URL for local development or separately hosted services.
BACKEND_URL=http://localhost:8080

# Paid Render services can alternatively use a private address. Set only one backend address.
# BACKEND_HOSTPORT=routeboard-api:10000

PORT=3000
HOSTNAME=0.0.0.0
