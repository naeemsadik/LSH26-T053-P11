# Public backend URL for local development or separately hosted services.
BACKEND_URL=http://localhost:8080

# Render injects this private address through render.yaml. Set only one backend address.
# BACKEND_HOSTPORT=routeboard-api:10000

PORT=3000
HOSTNAME=0.0.0.0
